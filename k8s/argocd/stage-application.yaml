# k8s/argocd/stage-application.yaml
apiVersion: argoproj.io/v1alpha1
kind: Application

metadata:
  name: myapp-stage
  namespace: argocd

spec:
  project: apna-project

  source:
    repoURL: https://github.com/Ponkoog-roy/apna_collage.git
    targetRevision: eks-gitops
    path: k8s/overlays/stage

  destination:
    server: https://kubernetes.default.svc
    namespace: stage

  syncPolicy:
    automated:
      prune: true
      selfHeal: true